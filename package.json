{
  "name": "splitsy",
  "version": "1.0.0",
  "description": "hr-rpp33-boc",
  "main": "index.js",
  "scripts": {
    "server": "npx nodemon server/server.js",
    "test": "jest --detectOpenHandles",
    "buildCICD": "cd client && npm i &&npm run build"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongodb": "^4.6.0",
    "mongoose": "^6.3.4",
    "nodemon": "^2.0.16"
  },
  "devDependencies": {
    "jest": "^28.1.0"
  }
}
